
<%= form_for($hequipc, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if hequipc.errors.any? %>
      <div id="error_explanation">
          <h2><%= pluralize(hequipc.errors.count, "error") %> prohibited this hequipc from being saved:</h2>

          <ul>
              <% hequipc.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
          </ul>
      </div>
  <% end %>

  <div class="section" id="page">

    <h1>メニュー項目を編集する</h1>

      <%= form.hidden_field :hshop_id %>

      <div id="field">
          <%= form.label :name do %><label for="labelx">:メニューカテゴリー名</label><% end %><br>
          <%= form.text_field :name, placeholder: '例）店舗外設備' %>
      </div>

      <div id="actions">
          <li><%= form.submit(:決定) %></li>
      </div>

      <%= link_to myhshop_myhshop_path(id:$hshop) do %>
          <div id="eback">編集を中止する</div>ß
      <% end %>

  </div>

<% end %>
