
<%= stylesheet_link_tag "selecter", :media => "all" %>

  <p id="notice"><%= notice %></p>

<%= content_for(:main)  do %>

<div id="main-feature">
  <%= stylesheet_link_tag "selecter", :media => "all" %>

  <h1>車・バイク・自転車カテゴリ―</h1>

  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% $dcategories.each do |dcategory| %>
        <tr>
          <td><div id="st1"><%= dcategory.name %></div></td>
          <td><div id="st2"><%= link_to 'Show', dcategory_path(id:dcategory) %></div></td>
          <td><div id="st2"><%= link_to '編集', edit_dcategory_path(id:dcategory) %></div></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <div id="new"><%= link_to '新しい車・バイク・自転車カテゴリ―', new_dcategory_path %></div>

</div>

<% end %>