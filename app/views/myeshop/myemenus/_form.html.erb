
<%= form_for($emenu, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if emenu.errors.any? %>
      <div id="error_explanation">
          <h2><%= pluralize(emenu.errors.count, "error") %> prohibited this emenu from being saved:</h2>

          <ul>
              <% emenu.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
          </ul>
      </div>
  <% end %>

  <div class="section" id="page">

    <h1>メニュー項目を編集する</h1>
      
      <%= form.hidden_field :emenuc_id %>

      <div id="field">
          <%= form.label :name do %><label for="labelx">メニュー項目名</label><% end %><br>
          <%= form.text_field :name, placeholder: '定食' %>
      </div>

      <div id="actions">
          <li><%= form.submit(:決定) %></li>
      </div>

      <%= link_to myeshop_myemenuc_myemenus_path do %>
            <div id="eback">編集を中止する</div>
      <% end %>

  </div>
  
<% end %>
