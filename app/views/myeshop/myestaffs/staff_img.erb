<%= content_for(:main)  do %>

	<%= form_for($dstaff, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>

	<%= render 'form2', dstaff: $dstaff %>

	<% end %>

<% end %>