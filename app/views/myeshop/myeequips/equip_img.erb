<%= content_for(:main)  do %>

	<%= form_for($eequip, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>

	<%= render 'form2', eequip: $eequip %>

	<% end %>

<% end %>