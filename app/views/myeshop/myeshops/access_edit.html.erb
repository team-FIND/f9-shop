

<%= content_for(:main)  do %>

	<%= form_for($eshop, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>
		<%= render 'shared/editer/eshop/access', eshop: $eshop %>
	<% end %>

<% end %>