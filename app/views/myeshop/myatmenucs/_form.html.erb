
<%= form_for($atmenuc, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if atmenuc.errors.any? %>
      <div id="error_explanation">
          <h2><%= pluralize(amenuc.errors.count, "error") %> prohibited this amenuc from being saved:</h2>

          <ul>
              <% atmenuc.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
          </ul>
      </div>
  <% end %>

  <div class="section" id="page">

    <h1>テイクアウトメニューカテゴリーを編集する</h1>

      <%= form.hidden_field :ashop_id %>

      <div id="field">
          <%= form.label :name do %><label for="labelx">メニューカテゴリー名</label><% end %><br>
          <%= form.text_field :name, placeholder: 'グランドメニュー' %>
      </div>

      <div id="actions">
          <li><%= form.submit(:決定) %></li>
      </div>

      <%= link_to myashop_myashop_path(id:$ashop) do %>
            <div id="eback">編集を中止する</div>
      <% end %>

  </div>

<% end %>
