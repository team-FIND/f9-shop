<%= content_for(:main)  do %>

	<%= form_for($cequip, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>

	<%= render 'form2', cequip: $cequip %>

	<% end %>

<% end %>