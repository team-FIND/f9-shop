<%= form_for($out, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if out.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(out.errors.count, "error") %> prohibited this menu from being saved:</h2>

      <ul>
      <% out.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="section" id="page">

    <h1>料理を編集する</h1>

              <%= form.hidden_field :amenu_id %>

                  <div id="field-pic">
                      <%= form.label :outimg1 do %><label for="labelx">写真1</label><% end %><br>

                      <label for="labely"><%= form.file_field :outimg1, id: :out_outimg1 , id: "file1" %></label>

                      <div id="thumb1" onClick="$('#file1').click()">
                            <% if out.outimg1.present? %>
                              <div id="updimg"><%= image_tag out.outimg1.thumb.url if $out.outimg1? %></div> 
                            <% else %>
                              <div id="upimg"><%= image_tag("template/img") %></div>
                            <% end %>
                      </div>
                  </div>

                  <div id="field-pic">
                      <%= form.label :outimg2 do %><label for="labelx">写真2</label><% end %><br>

                      <label for="labely"><%= form.file_field :outimg2, id: :out_outimg2 , id: "file2" %></label>

                      <div id="thumb2" onClick="$('#file2').click()">
                            <% if out.outimg2.present? %>
                              <div id="updimg"><%= image_tag out.outimg2.thumb.url if $out.outimg2? %></div> 
                            <% else %>
                              <div id="upimg"><%= image_tag("template/img") %></div>
                            <% end %>
                      </div>
                  </div>

                  <div id="field-pic">
                      <%= form.label :outimg3 do %><label for="labelx">写真3</label><% end %><br>

                      <label for="labely"><%= form.file_field :outimg3, id: :out_outimg3 , id: "file3" %></label>

                      <div id="thumb3" onClick="$('#file3').click()">
                            <% if out.outimg3.present? %>
                              <div id="updimg"><%= image_tag out.outimg3.thumb.url if $out.outimg3? %></div> 
                            <% else %>
                              <div id="upimg"><%= image_tag("template/img") %></div>
                            <% end %>
                      </div>
                  </div>


              <%= form.hidden_field :topimg_cache %>
              <%= form.hidden_field :outimg1_cache %>
              <%= form.hidden_field :outimg2_cache %>
              <%= form.hidden_field :outimg3_cache %>

              <div id="actions"><li><%= form.submit(:決定) %></li></div>

              <%= link_to myeshop_myemenu_myouts_path do %><div id="eback">編集を中止する</div><% end %>

</div>

<% end %>

<script>

$(function(){
  $fileField = $('#file1')
 
  // 選択された画像を取得し表示
  $($fileField).on('change', $fileField, function(e) {
    file = e.target.files[0]
    reader = new FileReader(),
    $preview = $("#thumb1");
 
    reader.onload = (function(file) {
      return function(e) {
        $preview.empty();
        $preview.append($('<img>').attr({
          src: e.target.result,
          width: "100%",
          class: "preview",
          title: file.name
        }));
      };
    })(file);
    reader.readAsDataURL(file);
  });
});

$(function(){
  $fileField = $('#file2')
 
  // 選択された画像を取得し表示
  $($fileField).on('change', $fileField, function(e) {
    file = e.target.files[0]
    reader = new FileReader(),
    $preview = $("#thumb2");
 
    reader.onload = (function(file) {
      return function(e) {
        $preview.empty();
        $preview.append($('<img>').attr({
          src: e.target.result,
          width: "100%",
          class: "preview",
          title: file.name
        }));
      };
    })(file);
    reader.readAsDataURL(file);
  });
});

$(function(){
  $fileField = $('#file3')
 
  // 選択された画像を取得し表示
  $($fileField).on('change', $fileField, function(e) {
    file = e.target.files[0]
    reader = new FileReader(),
    $preview = $("#thumb3");
 
    reader.onload = (function(file) {
      return function(e) {
        $preview.empty();
        $preview.append($('<img>').attr({
          src: e.target.result,
          width: "100%",
          class: "preview",
          title: file.name
        }));
      };
    })(file);
    reader.readAsDataURL(file);
  });
});

</script>

