
<%= form_for($admenuc, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if admenuc.errors.any? %>
      <div id="error_explanation">
          <h2><%= pluralize(admenuc.errors.count, "error") %> prohibited this admenuc from being saved:</h2>

          <ul>
              <% admenuc.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
          </ul>
      </div>
  <% end %>

  <div class="section" id="page">

    <h1>メドリンクカテゴリーを編集する</h1>

      <%= form.hidden_field :ashop_id %>

      <div id="field">
          <%= form.label :name do %><label for="labelx">メニューカテゴリー名</label><% end %><br>
          <%= form.text_field :name, placeholder: 'ソフトドリンク' %>
      </div>

      <div id="actions">
          <li><%= form.submit(:決定) %></li>
      </div>

      <%= link_to myashop_myashop_path(id:$ashop) do %>
            <div id="eback">編集を中止する</div>
      <% end %>

  </div>

<% end %>
