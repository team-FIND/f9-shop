<%= content_for(:main)  do %>

	<%= form_for($atakeout, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>

	<%= render 'form2', atakeout: $atakeout %>

	<% end %>

<% end %>