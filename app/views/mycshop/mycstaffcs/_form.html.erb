
<%= form_for($cstaffc, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if cstaffc.errors.any? %>
      <div id="error_explanation">
          <h2><%= pluralize(cstaffc.errors.count, "error") %> prohibited this cstaffc from being saved:</h2>

          <ul>
              <% cstaffc.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
          </ul>
      </div>
  <% end %>

  <div class="section" id="page">

    <h1>メニュー項目を編集する</h1>

      <%= form.hidden_field :cshop_id %>

      <div id="field">
          <%= form.label :name do %><label for="labelx">:メニューカテゴリー名</label><% end %><br>
          <%= form.text_field :name, placeholder: '例）従業員' %>
      </div>

      <div id="actions">
          <li><%= form.submit(:決定) %></li>
      </div>

      <%= link_to mycshop_mycshop_path(id:$cshop) do %>
          <div id="eback">編集を中止する</div>
      <% end %>

  </div>

<% end %>
