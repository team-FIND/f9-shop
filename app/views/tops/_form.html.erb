
<%= form_for($top, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if top.errors.any? %>

    <div id="error_explanation">

      <h2><%= pluralize(top.errors.count, "error") %> prohibited this top from being saved:</h2>

          <ul>
            <% top.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>

    </div>

   <% end %>

    <div class="field">
      <p id="st3">
        <%= form.label :name %><br>
        <%= form.text_field :name %>
      </p>
    </div>

    <div class="actions">
      <div id="new">
      <%= form.submit %>
      </div>
    </div>
  
<% end %>
