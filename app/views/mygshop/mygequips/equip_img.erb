<%= content_for(:main)  do %>

	<%= form_for($gequip, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>

	<%= render 'form2', gequip: $gequip %>

	<% end %>

<% end %>