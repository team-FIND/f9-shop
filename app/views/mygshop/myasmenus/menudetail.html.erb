

<%= content_for(:main)  do %>

	<%= form_for($asmenuc, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>
			<%= render 'form2', asmenuc: $asmenuc %>
	<% end %>

<% end %>