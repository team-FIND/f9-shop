

<%= content_for(:main)  do %>

	<%= form_for($gsmenuc, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>
			<%= render 'form2', gsmenuc: $gsmenuc %>
	<% end %>

<% end %>
