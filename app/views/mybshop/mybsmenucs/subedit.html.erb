

<%= content_for(:main)  do %>

	<%= form_for($bsmenuc, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>
			<%= render 'form2', bsmenuc: $bsmenuc %>
	<% end %>

<% end %>
