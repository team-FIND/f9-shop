

  <%= content_for(:head)  do %>
      <%= stylesheet_link_tag "shophome/ashop/content/menu", :media => "all" %>
      <%= javascript_include_tag "city" %>

      <title><%= page_title %></title>
  <% end %>


  <% @page_title = $bequipc.name  %> 
  <% @title1 = $bshop.shopname %> 
  <% @title2 = @page_title %> 

  <%= content_for(:edit) do %>
      <div id="edit2">
          <%= link_to '✐編集', subedit_mybshop_mybshop_mybequipc_path(mybshop_id:$bshop, id:$bequipc) %>
      </div>    
  <% end %>


  <%= content_for(:main)  do %>

        <% $bequipcs.each do |bequipc| %>

                <% if $bequipc.content.present? %>
                    <div id="content1"><%= simple_format(bequipc.content) %></div>
                <% else %>
                    <div id="content0">記載がありません</div>
                <% end %>
                
        <% end %>

  <% end %>
