<%= content_for(:main)  do %>

	<%= form_for($bequip, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>

	<%= render 'form2', bequip: $bequip %>

	<% end %>

<% end %>