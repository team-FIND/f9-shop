
	
<%= content_for(:main)  do %>

	<%= form_for($bshop, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>
		<%= render 'shared/editer/bshop/info', bshop: $bshop %>
	<% end %>

<% end %>