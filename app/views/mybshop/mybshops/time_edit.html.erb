
	
<%= content_for(:main)  do %>

	<%= form_for($bshop, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>
		<%= render 'shared/editer/bshop/time', bshop: $bshop %>
	<% end %>

<% end %>