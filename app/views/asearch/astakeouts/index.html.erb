
<%= content_for(:head)  do %>
    <%= stylesheet_link_tag "shophome/ashop/each", :media => "all" %>
    <%= javascript_include_tag "city" %>
    <title><%= page_title %></title>

<% end %>

<% @page_title = $atmenu.name %> 
<% @title1 = $ashop.shopname %> 
<% @title2 = $atmenu.name %>

<%= content_for(:main)  do %>

  <% $atakeouts.each do |atakeout| %>

        <div id="food">
          
                <div id="top_pic"> 

                    <% if atakeout.topimg.present? %>
                      <%= link_to image_tag(atakeout.topimg_url, {:size =>'300x300'}), asearch_atsmenu_astakeout_path(id:atakeout) %> 
                    <% else %>
                      <%= link_to image_tag("template/logo3", {:size =>'300x300'}), asearch_atsmenu_astakeout_path(id:atakeout) %> 
                    <% end %>
    
                </div>

                <P id="food_name1">
                    <% if atakeout.foodname1.present? %>
                        <%= atakeout.foodname1 %>
                    <% else %>
                        未入力（料理名１）
                    <% end %>
                </P>

                <P id="food_name2">
                    <% if atakeout.foodname2.present? %>
                        <%= atakeout.foodname2 %>
                    <% else %>
                        未入力（料理名２）
                    <% end %>
                </P>

                <P id="price">税込：<%= atakeout.price %>円</P>

         </div>


  <% end %>


<% end %>

