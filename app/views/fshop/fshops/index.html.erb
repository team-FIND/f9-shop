

  <% @page_title = "教育一覧" %> 
  <% @title = @page_title %>
  <% @back = area_prefec_path(id:$prefec, area_id:$area) %>


<%= content_for(:uheader)  do %><%= render 'shared/uheader2' %><% end %>


<%= content_for(:main)  do %>
    <%= stylesheet_link_tag "shop/shop", :media => "all" %>
    <%= stylesheet_link_tag "search/asearch", :media => "all" %>

  
   <div id="search-form">

      <%= search_form_for $search, url: fsearch_fsshops_path do |form| %>

              <div id="stitle">病院検索</div>
                          
                <div id="slinex">
                    <div class="sline1">
                      <label for="labelx">カテゴリー</label><br>
                    </div>
                </div>

                <div id="sliney">
                    <div class="sline2">
                      <label for="labelx">店名</label><br>
                      <%= form.search_field :shopname_cont %>
                    </div>

                    <div class="sline2">
                      <%= form.submit %>
                    </div>
                </div>

            <% end %>

         
    </div>

<div id="main-feature">
  <% $fshops.each do |fshop| %>


      <%= link_to prefec_fshop_fshop_path(prefec_id:$prefec, id:fshop) do%> 
         <div id="shopx">

                <div id="toppic">

                  <% if fshop.topimage.present? %>
                    <%= image_tag fshop.topimage_url %> 
                  <% else %>
                    <%= image_tag "template/logo3" %> 
                  <% end %>

                </div>

                <div id="explains">
                     
                        <P id="shop_name"><%= fshop.shopname %></P>

                        <P id="account_name"><%= fshop.user.username %></P><br>
                        
                        <li id="ptag">
                          <p><%= fshop.top.name %></p>
                          <p>><%= fshop.area.name %></p>
                          <p>><%= fshop.prefec.name %></p>
                        </li><br>

                        <li id="tag"><p>#<%= fshop.fcategory.name %></p></li>

                </div>

          </div>
        <% end %>

  <% end %>
  
</div>

<% end %>

