
<%= content_for(:main)  do %>

	<%= form_for($cmenuc, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>
			<%= render 'form2', cmenuc: $cmenuc %>
	<% end %>

<% end %>
