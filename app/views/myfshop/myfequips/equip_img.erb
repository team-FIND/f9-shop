<%= content_for(:main)  do %>

	<%= form_for($fequip, :url => {:action => "update"}, :html => {:multipart => true}) do |form| %>

	<%= render 'form2', fequip: $fequip %>

	<% end %>

<% end %>