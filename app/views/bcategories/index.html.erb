
<%= stylesheet_link_tag "selecter", :media => "all" %>

  <p id="notice"><%= notice %></p>

<%= content_for(:main)  do %>

<div id="main-feature">
  <%= stylesheet_link_tag "selecter", :media => "all" %>

  <h1>病院カテゴリ―</h1>

  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% $bcategories.each do |bcategory| %>
        <tr>
          <td><div id="st1"><%= bcategory.name %></div></td>
          <td><div id="st2"><%= link_to 'Show', bcategory_path(id:bcategory) %></div></td>
          <td><div id="st2"><%= link_to '編集', edit_bcategory_path(id:bcategory) %></div></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <div id="new"><%= link_to '新しい病院カテゴリ―', new_bcategory_path %></div>

</div>

<% end %>