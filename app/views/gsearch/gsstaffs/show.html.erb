
<% @page_title = $gstaff.name %> 
<% @title2 = $gstaff.name %> 

<% $gstaffs.each do |gstaff| %>

	<% if $gstaff.staff_img.present? %>
		<% @image1 = image_tag $gstaff.staff_img_url %>
	<% else %>
		<% @image1 = image_tag "template/logo3" %>
	<% end %>

	<% if $gstaff.explain.present? %>
			<p><%= $gshop.explain %></p>
	<% else %>
			<p>未入力</p>
	<% end %>

<% end %>