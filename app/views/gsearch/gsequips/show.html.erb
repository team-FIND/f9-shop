
<% @page_title = $gequip.name %> 
<% @title2 = $gequip.name %> 

<% $gequips.each do |gequip| %>

	<% if $gequip.equip_img.present? %>
		<% @image1 = image_tag $gequip.equip_img_url %>
	<% else %>
		<% @image1 = image_tag "template/logo3" %>
	<% end %>

	<% if $gequip.explain.present? %>
			<p><%= $gshop.explain %></p>
	<% else %>
			<p>未入力</p>
	<% end %>

<% end %>