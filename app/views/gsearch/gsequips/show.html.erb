
<% @page_title = $bequip.name %> 
<% @title2 = $bequip.name %> 

<% $bequips.each do |bequip| %>

	<% if $bequip.equip_img.present? %>
		<% @image1 = image_tag $bequip.equip_img_url %>
	<% else %>
		<% @image1 = image_tag "template/logo3" %>
	<% end %>

	<% if $bequip.explain.present? %>
			<p><%= $bshop.explain %></p>
	<% else %>
			<p>未入力</p>
	<% end %>

<% end %>