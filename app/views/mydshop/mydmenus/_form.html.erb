
<%= form_for($dmenu, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if dmenu.errors.any? %>
      <div id="error_explanation">
          <h2><%= pluralize(dmenu.errors.count, "error") %> prohibited this dmenu from being saved:</h2>

          <ul>
              <% dmenu.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
          </ul>
      </div>
  <% end %>

  <div class="section" id="page">

    <h1>メニュー項目を編集する</h1>
      
      <%= form.hidden_field :dmenuc_id %>

      <div id="field">
          <%= form.label :name do %><label for="labelx">メニュー項目名</label><% end %><br>
          <%= form.text_field :name, placeholder: '定食' %>
      </div>

      <div id="actions">
          <li><%= form.submit(:決定) %></li>
      </div>

      <%= link_to mydshop_mydmenuc_mydmenus_path do %>
            <div id="eback">編集を中止する</div>
      <% end %>

  </div>
  
<% end %>
