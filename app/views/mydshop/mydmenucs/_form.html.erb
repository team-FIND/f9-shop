
<%= form_for($dmenuc, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if dmenuc.errors.any? %>
      <div id="error_explanation">
          <h2><%= pluralize(dmenuc.errors.count, "error") %> prohibited this dmenuc from being saved:</h2>

          <ul>
              <% dmenuc.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
          </ul>
      </div>
  <% end %>

  <div class="section" id="page">

    <h1>メニュー項目を編集する</h1>

      <%= form.hidden_field :dshop_id %>

      <div id="field">
          <%= form.label :name do %><label for="labelx">:メニューカテゴリー名</label><% end %><br>
          <%= form.text_field :name, placeholder: 'グランドメニュー' %>
      </div>

      <div id="actions">
          <li><%= form.submit(:決定) %></li>
      </div>

      <%= link_to mydshop_mydshop_path(id:$dshop) do %>
          <div id="eback">編集を中止する</div>
      <% end %>

  </div>

<% end %>
