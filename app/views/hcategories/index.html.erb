
<%= stylesheet_link_tag "selecter", :media => "all" %>

  <p id="notice"><%= notice %></p>

<%= content_for(:main)  do %>

<div id="main-feature">
  <%= stylesheet_link_tag "selecter", :media => "all" %>

  <h1>グルメカテゴリ―</h1>

  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% $hcategories.each do |hcategory| %>
        <tr>
          <td><div id="st1"><%= hcategory.name %></div></td>
          <td><div id="st2"><%= link_to 'Show', hcategory_path(id:hcategory) %></div></td>
          <td><div id="st2"><%= link_to '編集', edit_hcategory_path(id:hcategory) %></div></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <div id="new"><%= link_to '新しいサブカテゴリ―', new_hcategory_path %></div>

</div>

<% end %>