
<%= form_for($area, :url => {:action => "create"}, :html => {:multipart => true}) do |form| %>

  <% if area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
      <p id="st3">
        <%= form.label :top_id %><br>
        <%= form.text_field :top_id %>
      </p>
    </div>

   <div class="field">
      <p id="st3">
        <%= form.label :name %><br>
        <%= form.text_field :name %>
      </p>
    </div>

    <div class="actions">
      <div id="new">
      <%= form.submit %>
      </div>
    </div>
  
<% end %>

